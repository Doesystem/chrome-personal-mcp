services:
  agent:
    build: .
    container_name: chrome-personal-mcp
    environment:
      - MODE=debug    # set to debug to enable VNC
    volumes:
      - ./data:/data
    ports:
      - "5900:5900"  # VNC — only used when MODE=debug
    shm_size: '1gb'
