services:
  agent:
    build: .
    container_name: chrome-personal-mcp
    environment:
      - MODE=prod    # เปลี่ยนเป็น debug เพื่อเปิด VNC
    volumes:
      - ./data:/data
    ports:
      - "5900:5900"  # VNC — ใช้เฉพาะ MODE=debug
    shm_size: '1gb'
